# Current stable implementation checkpoint

Source: `d99e5795269d6638306f897d964ba3e9d048af712796ec2390850a0f2041b7a6`. Candidate: `stable-20260906-050731`. Production/tests/harness/fixtures remain frozen; only excluded evidence/reports/notes can change.

Root executions completed against this exact tree and immutable app/parser image IDs:

- Full pytest: 451 passed, zero failures/errors/skips, 434.344 seconds, record `f97ee038`. Exact current test-file hashes and every node supporting the 71 registered scenarios verified. JUnit SHA `18106ff73def4d96755ab0e8099e30ea1f09f6dbfee764bca2c1700017760c75`.
- Deterministic two-template repeated build/HTML/ZIP bytes: `f6e9f36e`.
- Fresh-volume offline cold start, real PDF import, corruption/readiness recovery, exact backup/restore: `c0d591fd`; actual four-service image/health/network/ports inspected for both projects. Does not prove a clean host.
- Real worker SIGKILL before publication pointer: `03649701`.
- Actual schema 5 to 10 upgrade and maintenance recovery: `d53f8755`.
- Actual browser close at 3/10, worker SIGKILL and resume to 10/10 with the explicit FakeProvider: `92827f9c`; already completed blocks not repeated. No real Provider execution.
- Production validators reject four attack forms with zero socket attempts: `2d023333`.
- Readiness full three-branch evidence combination: `04649d50`, with current native/missing-model parser executions `3eedb1b5`/`c6773c37`.

Browser and parser agents are completing fresh full combinations and independent mixed-scenario reviews. The centralized report must be regenerated after those records land. Two actual UI findings (legacy source locator notice and semantic source/target quotations) were fixed before this freeze; current immutable-image counterchecks passed. Historical failures remain retained.

External prerequisites remain unfulfilled: approved fixed model/profile/key-file path/shared test budget/controlled-text dispatch authorization; and an available clean/new Docker-only host. Six scenario blockers are explicitly registered for this tree (`5ee7be13`, `f6e6ab5f`, `1f44d8a8`). They cannot be replaced by FakeProvider, old proofs or same-host fresh volumes. No full M0/M1/M2 completion or release approval is claimed.

All manual/visual/source acceptance is assigned to independent agents. Implementation/ownership/evidence handoffs use project files. Original localhost8080 is untouched; temporary test projects are shut down after review, named recovery volumes retained.
