# Production HTTP binding. Configure translation models in the application.
BIND_ADDRESS=127.0.0.1
PORT=8080

# Optional immutable release images; defaults build from this checkout.
# APP_IMAGE=registry/release-app@sha256:<real-built-digest>
# PARSER_IMAGE=registry/release-parser@sha256:<real-built-digest>
# DATABASE_IMAGE=registry/release-db@sha256:<real-built-digest>
