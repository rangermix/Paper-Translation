# Independent new-host environment checkpoint

Reviewer `/root/ir_publisher_parser`; root provisions and operates the environment. Tracked source remains frozen d99e5795269d6638306f897d964ba3e9d048af712796ec2390850a0f2041b7a6. Only excluded evidence/notes changed.

The new OS candidate is WSL2 `BiblioCleanD99`, imported from a stopped official Docker29-dind Alpine rootfs into `.local-data/wsl-hosts/BiblioCleanD99`. This is a new root filesystem/Engine, not a privileged DinD container. It shares the existing WSL2 kernel and physical Windows machine. Rootfs archive SHA1b83f3abf18e2c1b3b403478fd7546f816e38ca99e06bda10adf30b5fa9c7514 independently recomputed; actual new root ext4/dev/sdg, device8:96.

Firstboot independent29+14 raw probes: `evidence/new-host-independent/BiblioCleanD99-firstboot` and `BiblioCleanD99-path-followup`; review `BiblioCleanD99-firstboot-review.json`. All19 Python/Node/qpdf/PostgreSQL/Docling/OCR/CUDA commands absent, binary search empty and actual Alpine package inventory has no app runtimes. Docker29.8.0/Compose5.5.1 static Linux tools present. No Docker daemon, local socket or Docker/containerd state existed before bootstrap. Initial direct `wsl --exec` omitted/sbin tools; original failures retained, shell-PATH followup succeeded without installs.

Important actual finding: user WSL default netns4026531840 contains preexisting libvirt rules/virbr0/ip_forward1/service listeners. It differs from Desktop daemon netns4026532231 but is not empty. Root accepted reviewer recommendation: do not modify it or run standard Docker rules there. New dockerd starts under `unshare -n` with standard rules only in new private netns4026532891, initially onlylo/private docker0 and connected172.30.192/24 route, no default/uplink. Shared userWSL rule definitions (normalized counters/time only), links/routes remain equal. No global shutdown or old daemon modification was done by reviewer.

Before-load review: `evidence/new-host-independent/BiblioCleanD99-beforeload-review.json`, with32 successful read-only probes in adjacent directory. Actual local dockerd PID9, new EngineID27d192f4-781d-4c84-9ec4-64adc6319e3d; Docker images0/containers0/volumes0 observed before release transfer. `/var/run/docker.sock` kernel inode4141707 attributed to actual PID9 FD and private `/proc/9/net/unix`; no Desktop proxy. DockerRootDir `/var/lib/docker`, actual containerd root `/var/lib/docker/containerd/daemon`, both new ext4root. Package database unchanged. Root was told this prerequisite is verified and can copy/load release archives.

WSL settings request no automount/fstab/interop/WindowsPATH; actual Windows drives/PATH absent. Shared WSL platform mounts still expose Desktop guest sockets under/mnt/wsl, and global WSLInterop binfmt/init socket remain present although WSL_INTEROP env unset. Do not claim these shared mechanisms are absent. Actual commands use only the new Linux binaries and explicit local Unix Engine socket. No Windows executable runs inside the distro; no old host code/model/runtime mounts allowed.

Four public bundle helpers independently source-reviewed and byte-snapshotted: `evidence/new-host-independent/bundle-source-review/INDEPENDENT_SOURCE_REVIEW.json`. Original Desktop backup is copied from readonly source and verified twice without overwrite; restore holds maintenance, keeps synthetic unknown80000/onePermit/no usage/request_id, then resumes only local pending index. All Python executes inside immutable release images, never the new OS host. Added `new-host-cold-parse.py` must execute real worker/spool Docling four-block controlledPDF and0Permit; source review alone does not imply execution. HTTP bridge remains non-internal, so actual no-uplink private namespace supplies the offline envelope; do not misdescribe every bridge as internal.

Next: root loads app/parser/database archives and runs release doctor/start/import/read/export/recreate/restore/coldparse. Reviewer still needs actual image/mount/model/cgroup/network and post-run host-package checks. QA browser will run in a separate prebuilt tool container using this Engine's host network (the private namespace); Node/Chromium only there, not installed on OS or counted as product services. No environment certificate has been imported as a complete M0AT18A/M1AT25A pass yet. No Provider authorization/call or new physical-machine assertion.

## Actual runtime checkpoint, 2026-09-06 06:11 UTC

`evidence/new-host-independent/BiblioCleanD99-running-m0-review.json` records another41 read-only actual commands plus the corrected readiness request. All four product services healthy/nonroot/read-only/dropALL; app90/parser25 deployed source hashes independently recomputed equal to frozen d99. No `/app`/model/socket overlay; parser only inputs/outputs mounts and network none. Full local models verified before inference; actual memory.max4294967296, pids.max128, AS unlimited. Kernel swap control is unsupported and memory.swap.max=max: **do not claim a swap cap**. Exact Engine localID remains27d192f4..., Compose actual5.5.1. Shared links/routes/IPv4/IPv6 rules and host package DB remain equal to beforeload; all19 app runtime commands still absent. Private namespace has only localDocker networks and no uplink/default; IPv4/IPv6 documentation-address route lookup explicitly returns Network unreachable. HTTP bridge is non-internal but has no path beyond the isolated daemon namespace. Reviewer initially queried `/readyz` and received SPA HTML; this is retained as a wrong observer URL and not counted. Separate actual `/health/ready` returned validreadyJSON.

QA tooling source review is complete: `evidence/new-host-independent/browser-tool-review-final/INDEPENDENT_SOURCE_REVIEW.json`. Initial ZIPhelper boundedness finding retained in the earlier snapshot. Web fixed preallocation cumulative declared size/entry checks; independent exactimage b8a2c3... networknone execution rejected traversal/two41MiB/one81MiB/1025entries with no destination writes, and extracted validUnicodeZIP exactly. This certifies the QA helper read review and focused execution, not product browser behavior.

Root approved new-host four frozen-corpus cold runs because one four-blockEN case alone must not be called the whole frozen corpus. Prepared `evidence/new-host-cold-corpus/stable-d99-cold-corpus.tar`, SHA88ba21d9e6860e6f9f3358d23d03be85b626c600160a9115db0c51f7e59a3172; root executes it, acceptance independently reviews the excluded helpers. Each test-only temporary parser container uses same immutable a5aaimage, fresh512MiB tmpfs/empty cache,4GiB actualmemory/pids128/networknone/noDBorProvidersecret, and existing unchanged harness inside entrypoints invoking productionrun_once/spawn. Before/after source/model/cgroup/results bound, actual rawcoverage is preserved (crosspage/Efficient still needsreview), not correctedgold. No execution claim yet. Product deployment remains the four-service Compose stack; these are bounded additional QA processes, not an alternative deployment.

## Completed new-host review, 2026-09-06 06:33 UTC

Root executed all four cold cases and exported `evidence/new-host-cold-corpus/root-execution/target-results.tar`, SHA59d5e08da4101bcd0881146ab0d6145d4a7acad89890fb95cd26b13ee428c05a. Final actual review and independent212-file/tarmember nativeSHA verification: `evidence/new-host-independent/cold-corpus-actual-review-final/`. All42pagePNGs and all sourceassets compare directly byte-for-byte to previous immutable sameimage actualraw outputs; allinspection/coverage exact. SourceAST is fully equal exceptcreated_at and explicitcrosspageund→en/config/id. Actualmemory peaks: two-column938807296, crosspage1032237056, Efficient2016739328, Pathways1953644544 bytes; OOM0. Each freshcache/model/package/runtime check passed. These remain raw outputs, not correctedgold.

An observer/harness evidence timing issue is preserved: each innercold-result captured the still-open outerrun.stdout before the helper's lastsummary print. Four innerlogSHA values are exactprefixes of final logs; all other innerfilesSHA remainedstrict. Original failure at `cold-corpus-actual-review/failure.json`; originaltar and innerreports untouched. Finalnative212SHA and tar bytes separatelyverify all finalizedlogs. Do not claim the innerloghashes were finalized or silently replace them.

Actual independently opened PostgreSQL transactions explicitly `SET TRANSACTION READ ONLY` and `SHOW...=on` both before and after release: `migration-held` / `migration-resumed` rawrecords. Heldworkerwasrunning butlocaltaskpending/fence0; maintenance/dispatchtrue; unknown80000/Permit1/4Attempts/no requestID/usage. Afterrootrelease, exactlyone localtask succeeded/fence1/attempt1 andtotalAttempts5, allunknownstate/risk/Permit unchanged; all11originalrestoredfileSHA recomputed equal in both phases. Syntheticunknown remains labelled, no actualProviderclaim.

Final26read-onlyprobes in `BiblioCleanD99-final-stopped/`: newEngineallstatecontainers0,20namedvolumesretained; hostall19commandsabsent/packageDBunchanged; sharedlinks/routes/IPv4/IPv6rules equal; privateNSnounlink/default/app18096listener; originalDesktop8080/55439stillrunning. Root receivedpermissiontostopowneddaemon onlyafter evidenceexport. It SIGTERMedPID9/sessionexit0, exportedfinaldaemonlog, terminatedonlyBiblioCleanD99. VHDX10.6GB and20volumes retained; Ubuntu/Desktopstillrunning. **Do not run any new wsl command targeting this stopped distro.** Cleanuprecords `evidence/new-host-bootstrap/stable-d99/cleanup/`.

Formal independent manual/environment certificate prepared and given toacceptance forimport: `evidence/reviews/new-host-independent-environment-migration-d99.json`, SHA888af671ad4ca7d4e922e4626a2c39377ac56ec8d240205a01789c16873f5dd5,332boundevidencefiles,M0AT18A/M1AT25A. Environment/runtimeoperatorsareotheragents; ownadditionalhelper explicitlyhasacceptanceindependentreview. Browseractuals andfullComposeaggregate are peerrecords, not claimed as ours. Alllimits above remain in certificate. Trackedproductsource untouched atd99; actualProviderrequirements remain unresolved separately.
