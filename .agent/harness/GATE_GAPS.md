# Current gate gap audit

Updated 2026-09-06 by `/root/acceptance_harness`. This replaces the earlier working
list of implementation gaps; all original failed execution records remain intact.
It is not milestone sign-off. `.agent/tmp/evidence/observed-scenario-map.json` joins scoped
observations, while `.agent/tmp/evidence/formal-gate-gap-report.md` preserves every literal
scenario and its owners' qualifications. Neither file promotes an old success to
a current source-bound certificate.

## Current implementation findings and open work

- Root fixed the actual zero-issue semantic-review stale-status defect and added
  explicit not-requested/incomplete/stale/completed status with selected block
  scope. Independent 8 real PostgreSQL/API cases passed, including edits while a
  response arrives and unselected-block changes. The new image/browser check is
  pending. This is an optional simulated review contract, not semantic certification.
- Root fixed the previously inaccessible cleanup status. A tombstone exposes only
  a minimal receipt; payloads, task results, Attempt/Permit evidence, old Events,
  original links and controls remain unavailable. Independent 4 PostgreSQL cases
  passed, including poisoned retained metadata and all controls returning 410.
  The web agent's earlier real DELETE removed the files and content rows, retained
  the other document, and left its 15-file backup verifiable. New UI projection
  needs an updated image and actual browser confirmation.
- The source workbench is receiving the last small UI entry points for already
  validated native-span normalization and continuation merge. Backend exact-span,
  occurrence, protected-atom and float-order guards passed independent negative
  and positive tests. Wait for the UI author's freeze and independent review.
- M1-AT05A still needs an actual parsed cross-page table fixture with independently
  checked ownership/page/asset handling. The authored M0 merged table is insufficient.
- M1-AT14A now has a passed combined actual lost-response SIGKILL, natural lease
  recovery, independent UI .agent/tmp/evidence/risk acknowledgement, and explicit retry of
  that same task. The exact run `.agent/tmp/evidence/unknown-risk-ui/0f46c074` preserves old unknown20480 and usageNone; only after independent UI .agent/tmp/evidence/risk acknowledgement does a new fence settle150, with two total Fake calls and one target version. Its Provider is explicitly simulated, with an empty key.
- M1-AT22A comprehensive success/failure/retry/cancel instrumented log and export sweep now passed, with an independent six-test rerun. `.agent/tmp/evidence/reviews/content-privacy-draft-title-independent-review.md` records the scope: logs omit full source blocks and keys; legitimate reading-export body text remains. The run found and fixed a missing-title draft500; the updated image still needs that renderer/snapshot change.
- No real Provider request is authorized or performed. Actual fixed-model request
  IDs/usage and independent bilingual risk review remain unavailable until the user
  supplies explicit scope authorization, a fixed public profile, backend secret
  path, and total test budget. The default-off runner validates those prerequisites
  without reading secret values or executing a request. Simulated tests never pass
  a live-provider gate.
- A separate clean Docker-only host has not been provisioned. Read-only `Get-VM`, `Get-VMHost` and `Get-VMSwitch` are denied to the current user; QEMU/VirtualBox are not in PATH. Evidence: `.agent/tmp/evidence/reviews/clean-host-capability-20260906.md`. Existing WSL distributions were left unchanged. Fresh fully isolated
  Docker projects use image-contained application runtimes and restore into new
  PostgreSQL/data volumes, but the orchestration host has Python/Node. Keep the
  literal new-host portions of M0-AT18A and M1-AT25A unpassed.

## Evidence now available, formerly listed as missing

| Area | Concrete observed behavior and retained evidence |
|---|---|
| M0 input and identity | Actual PDF-only API, malformed/IR/legacy negative matrix, no identity entities, frozen CSS and two known legacy pages; `.agent/tmp/evidence/reviews/m0-acceptance-map.json`. |
| M0 static publication | Authored two-page/29-object fixture, true 2x2 and merged tables, long code/title, nested ToC, both templates and exports at 320px/no-JS/denied storage; independent `.agent/tmp/evidence/complex-reader-reviewed/INDEPENDENT_REVIEW.md`. Authored IR is not parser gold. |
| PDF local safety | Real encrypted/201-page/oversized geometry/100MP/malformed/scan/mixed OCR worker matrix passed; `.agent/tmp/evidence/pdf-security-matrix/706abb5c`. Real cold action-PDF Docling parse with two listener positive controls and zero PDF-path hits passed in `.agent/tmp/evidence/action-probe/63580b6d61`; parser network-none, local-model SHA health, no secret environment. This is not syscall tracing. |
| Source correspondence | Both research-paper final corrected source revisions have complete independent original-page gold: Efficient `b74b4299...`, Pathways `293a7507...`. Actual API corrections and exact pixel evidence are retained. Raw parser failures are not retroactively passed; cross-page-table literal remains separate. |
| Upload/no Provider | `.agent/tmp/evidence/no-provider-runtime/6cde038e`: actual two-chunk interruption, replayed finalize/import, exactly one real Docling parse/four exact blocks, missing-profile rejection and empty-key waiting_config with zero permits. `.agent/tmp/evidence/upload-batch/85350bd3`: three valid/one invalid, same-name independence, renamed duplicate and explicit reuse CAS. |
| Translation structure/cache | Real PostgreSQL oversized paragraph with intact formula, concurrent out-of-order unit results and one final assembly, new-job/new-document persistent cache and five independent invalidation axes. Independent results `long-formula-independent.xml` and `cache-order-glossary-independent.xml`. All Provider responses are explicit test doubles. |
| Retry and costs | Two production-adapter MockTransport 429 Retry-After 3s/5s responses persist real due times and release permits before third success. Permanent401 stops; missing/duplicate/truncated/forged-path paid test outputs permit one bounded repair then stop. `provider-retry-wire-bounds-independent.xml` has 7 passes and no external transport. |
| Lifecycle and deletion | Real index/cache FK deadlocks were reproduced and fixed with maintenance-to-global lock order; late money after DELETE/cleanup settles without resurrecting content. Independent backup-lock blocking, parse/publish/export late-write races and file-copy hash revalidation evidence remain. |
| Editor and locales | Actual 100 blocks/98 locked, exact two-block candidate acceptance, three changed glossary terms affecting two blocks, source/glossary lock scope and CAS/ABA. Experimental ja/zh-Hant keep independent HTML locale/pointers/cache while sharing one source and no parser calls. These do not certify additional language model quality. |
| Template batch | Twenty actual API-to-worker replacements with one corrupt private PNG: only that document fails and retains old pointer; 19 advance, all20 pointers checked after every task, all120 old files unchanged, 0 Provider/cache writes. `rebuild-batch-failure-independent-first.xml`. |
| Restore and upgrade | `.agent/tmp/evidence/offline-compose/runs/fca35c8d`: cf026 fresh cold start, four legacy exports, corruption503/recovery200 and new-volume restore; all projects cleaned. `.agent/tmp/evidence/schema-upgrade/9c6afc0d`: actual pre-release schema5 app backup containing source/sealedTR/old artifact/ZIP/metadata/pending task/synthetic unknown80000 restored through schema10 with11 exact files, held dispatch then one local resume. Independent web review passed. This is not a released-M1 or new-host claim. |

## Current release binding and dependencies

The inspected app `cf026d80...`, parser `7e2732e...` and database `04a249fe...`
match their recorded scan archives, SBOMs and runtime evidence. The app's
network-none packaging smoke checked schema10, parser manifest and 90 files against
host hashes. It predates the new semantic/cleanup/workbench UI changes and cannot
be called the final current application.

`.agent/tmp/evidence/release/current-review-20260906-manifest.json` deliberately has
`release_approved=false`. App/parser scans each retain 3 critical and 51 high OS
findings with no fixed version in that scan; database retains 13 critical and 73
high. No Python package findings were reported, but bundled native FFmpeg/OpenCV
OpenSSL1.1.1k linkage is an explicit additional limit. Zero Python findings is not
a clean native/OS security verdict. A source-only rebuild requires an honest new
image binding; do not relabel old scan files.

The prior full suite passed 363 tests with zero failures/skips, but its acceptance
record correctly failed source stability because `apps/web/WORK_LOG.md` changed.
That original record is preserved. The fingerprint now excludes exactly this pure
work log, while neighboring web source and README mutations still change it (13
harness integrity tests passed). Subsequent genuine production changes still
require a final stable-source run. All tests, harness scripts and docs must settle
before that single coordinated build/registration pass.

No historical observation, dependency scan, mock Provider, authored fixture or
successful container start automatically marks M0, M1, M2 or an inherited gate
complete. Final registry entries must identify exact commands, source/image hashes,
applicable independent agent review, and any unfulfilled literal condition.
