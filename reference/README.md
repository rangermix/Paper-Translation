# 阅读样式与旧版资料

`reader-v1.css`逐字节保留已提供的样式。`reference-files.sha256.json`列出本包中的实际相对路径与哈希；旧页面只调整返回新原型的导航，不重写正文或伪造来源定位。两篇论文及原PDF位于 `prototype/reader/`，嵌图单文件位于 `prototype/exports/`。

所有旧译读说明保留。旧页面不是严格原文/翻译准确性金标准；全文正式翻译必须从PDF重新走M1。这里是受控参考资料，不提供任意HTML导入。
