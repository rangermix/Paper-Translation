# These are REQUIRED built release images, not existing images supplied by this spec packet.
# APP_IMAGE=registry/release-app@sha256:<real-built-digest>
# PARSER_IMAGE=registry/release-parser@sha256:<real-built-digest>
# DB_IMAGE=postgres:<verified-major>@sha256:<real-verified-digest>
BIND_ADDRESS=127.0.0.1
PORT=8080
# External API requests are enabled/paused in Settings and persist in PostgreSQL.
