# Remaining live acceptance gap review

Read-only review by `/root/acceptance_harness`, 2026-09-06. No secrets opened, no requests sent, no tests rerun, no report regenerated. Current frozen source is `d99e5795269d6638306f897d964ba3e9d048af712796ec2390850a0f2041b7a6`.

**Conclusion:** the immediate blocker remains explicit external-processing authorization plus a fixed public model/profile, total budget and backend key-file path. No further already-authorized local product check was identified that can complete these four scenarios without those inputs. This does **not** mean that entering a key alone completes the product: live execution, independent semantic review and the subsequent publishing/multilingual sequence remain real work.

| Scenario | Literal requirement and remaining work |
|---|---|
| M1-AT08A | `milestones/M1-spec.md:121-125`: fixed model, structured response, real request ID/status/usage, no tools. The controlled runner can execute this first phase once authorized. Local MockTransport evidence is not a real response. |
| M1-AT08B | `milestones/M1-spec.md:127-129`: invalid key/nonexistent model/unsupported output capability must stop without model substitution. The existing success-path runner has no dedicated live negative-case entry. `tests/unit/test_openai_responses.py:31-34` covers synthetic HTTP 401 only. Any real negative request also needs explicit authorized scope; it is not authorized merely because it is expected to fail or cost zero. A bounded negative probe must be selected against the approved profile, not disguised as a successful eight-block run. |
| M1-AT26A | `milestones/M1-spec.md:335-345` and `milestones/M1-plan.md:330-334`: authorized en↔zh-Hans sample plus independent high-risk semantic review and source/output/usage evidence. The runner stops at `executed_requires_independent_review` and explicitly creates no human review or publication (`harness/live_provider_product.py:101-110`). Actual returned text is required before review/QA/seal/publish/export continuation (`harness/LIVE_PROVIDER.md:48-53`). |
| M2-AT21A | `milestones/M2-spec.md:275-285`: ordered source correction, locked partial retranslation, two target languages, semantic check, conflict, rebuild, rollback and offline export, while all earlier gates pass. `shared/verification-strategy.md:48-50` also requires independently reviewed controlled samples for added M2 languages. Existing local compound checks remain valid; the live corpus only permits en↔zh-Hans and therefore does not certify added Japanese/other language pairs. Authorized language scope and its reviewed outputs must precede the final cumulative sequence. |

Current report: 126 passed / 4 blocked. Exact remaining entries: `evidence/acceptance-report.json:702`, `:712`, `:1157`, `:1694`. Non-passed gates are M1-G03 (`:1861`, missing live_provider), M2-G04 (`:2025`, missing live_provider), and cumulative M2-G08 (`:2085`, browser/live inherited completion); `completion_claim_allowed` remains false (`:2105`). New-host prerequisites have actual passing evidence and are no longer a blocker.

## Ready entry and exact scope

`harness/LIVE_PROVIDER.md:8-26` documents the three commands: no-argument status; `--approval <path>` validation without execution; and the separate `--approval <path> --execute` operation. Only the last can dispatch. `harness/live_provider_run.py:58-81` checks explicit approval, one-use ID, exact manifest hash, public-profile hash, positive USD-micro budget and key-file existence without reading key bytes. No approval argument returns `not_authorized` (`:90-98`). One-use claim and retained uncertain state are implemented at `:101-106` and `:134-136`.

Approved-content manifest: `fixtures/live-provider/manifest.json`, SHA256 `b92ca3816e9f7ea1a28319b96a0c5a74ff3d4c6f436f2248600a3b6d89da2790`. It contains two one-page PDFs, four blocks each, testing number 64, negation, conditional unknown outcomes and a limiting claim. EN PDF SHA `16601b66faeaea415f9052d1bb8a5af4bf684144586ad067fcf5de664abc685d`; ZH PDF SHA `aa63e91a7a03dd8dc9f03973b6882219025518565222b79a8621f395e7556c12`. Both embedded `external_dispatch_authorized` values remain false (`:31`, `:207`); the manifest itself grants no authority. The explicit approval is a separate document.

When authorized, pin `ACCEPTANCE_APP_IMAGE` and `ACCEPTANCE_PARSER_IMAGE` to the verified IDs in `evidence/release/stable-20260906-050731/request.json:10-17`; the runner otherwise has historical candidate defaults (`harness/live_provider_run.py:120-121`). Inspect the effective DB image as well. Use the approved public profile and exact content scope; never guess model prices, credentials, a USD1 budget or extra language approval. `harness/LIVE_PROVIDER.md:30-40` describes the shared budget and bounded translation/candidate/semantic phase.

Parent's current authorization probe is already preserved at `evidence/goal-blocker-audits/20260906T064603Z/audit.json`: no approval/execute argument, `not_authorized`, zero network requests. I did not repeat it. The incomplete live-negative and multilingual continuation orchestration is an explicit scope limit, not a new production defect established by this review; preparing more wrappers now would not satisfy the missing real evidence or substitute for user input.
